// Normal exports
export let utilString = "utility string";
export let anotherString = "Another String"

// by adding default (only one per file) we are saying the when importing 
// this file, the is the main thing that we are importing 
// Exporting default is useful, when there is only one thing in the file that
// You want to export
export default "default string";